[project]
name = "notebooklm-slack-cockpit"
version = "0.1.0"
description = "Socket Mode Slack cockpit for safe NotebookLM CLI queries"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1,<2",
  "slack-bolt>=1.21,<2",
]

[tool.uv]
package = false
